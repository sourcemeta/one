{
  "$schema": "https://schemas.sourcemeta.com/self/v1/schemas/meta",
  "title": "Sourcemeta One Schema Statistics RPC Input",
  "description": "Get keyword statistics by vocabulary about every URI associated with a schema in the catalog",
  "examples": [
    {
      "schema": "https://example.com/foo"
    }
  ],
  "type": "object",
  "required": [ "schema" ],
  "properties": {
    "schema": {
      "description": "Absolute URI of a JSON Schema in this catalog",
      "type": "string",
      "format": "uri"
    }
  },
  "additionalProperties": false
}
