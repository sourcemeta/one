{
  "$schema": "https://schemas.sourcemeta.com/self/v1/schemas/meta",
  "title": "Sourcemeta One Schema Dependencies RPC Input",
  "description": "Get all direct and indirect dependencies of a schema in the catalog",
  "examples": [
    {
      "schema": "https://example.com/foo"
    }
  ],
  "type": "object",
  "required": [ "schema" ],
  "properties": {
    "schema": {
      "description": "Absolute URI of a JSON Schema in this catalog",
      "type": "string",
      "format": "uri"
    }
  },
  "additionalProperties": false
}
