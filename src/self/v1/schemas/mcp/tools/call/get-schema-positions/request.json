{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Sourcemeta One Schema Positions RPC Input",
  "description": "Get line and column position information for every token in a schema",
  "examples": [
    {
      "schema": "https://example.com/foo"
    }
  ],
  "type": "object",
  "required": [ "schema" ],
  "properties": {
    "schema": {
      "description": "Absolute URI of a JSON Schema in this catalog",
      "x-format-assertion": true,
      "type": "string",
      "pattern": "^[^#?]*$",
      "format": "uri"
    }
  },
  "additionalProperties": false
}
