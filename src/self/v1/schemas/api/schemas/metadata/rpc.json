{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Sourcemeta One Schema Metadata RPC Input",
  "description": "Get metadata information about a schema in the catalog",
  "examples": [
    {
      "schema": "https://example.com/foo"
    },
    {
      "schema": "foo"
    }
  ],
  "type": "object",
  "required": [ "schema" ],
  "properties": {
    "schema": {
      "description": "Absolute URI or relative path of a JSON Schema in this catalog",
      "$comment": "TODO: Use Format Assertion when supported on Blaze",
      "type": "string",
      "format": "uri-reference"
    }
  },
  "additionalProperties": false
}
