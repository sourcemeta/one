{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Sourcemeta One Schema Evaluate RPC Input",
  "description": "Evaluate a JSON instance against a schema in the catalog and return whether it is valid plus any errors",
  "examples": [
    {
      "schema": "https://example.com/foo",
      "instance": "hello"
    },
    {
      "schema": "foo",
      "instance": {
        "name": "Alice",
        "age": 30
      }
    },
    {
      "schema": "https://example.com/foo",
      "instance": 42
    }
  ],
  "type": "object",
  "required": [ "schema", "instance" ],
  "properties": {
    "schema": {
      "description": "Absolute URI or relative path of a JSON Schema in this catalog",
      "$comment": "TODO: Use Format Assertion when supported on Blaze",
      "type": "string",
      "format": "uri-reference"
    },
    "instance": {
      "description": "The JSON value to validate against the schema",
      "$ref": "./request.json"
    }
  },
  "additionalProperties": false
}
