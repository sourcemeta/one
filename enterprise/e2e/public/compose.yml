services:
  sandbox:
    build:
      context: ../../..
      dockerfile: enterprise/e2e/public/Dockerfile
      args:
        SOURCEMETA_ONE: one
    environment:
      - SOURCEMETA_ONE_PORT=8001
    env_file:
      - environment
    ports:
      - "${PORT}:8001"
