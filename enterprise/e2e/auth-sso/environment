ONE_E2E_OIDC_CLIENT_SECRET=registry-client-secret
ONE_E2E_OIDC_SESSION_SECRET=a-session-signing-secret-for-the-sso-sandbox
ONE_E2E_OIDC_CLEARTEXT_CLIENT_SECRET=a-client-secret-for-the-cleartext-provider
ONE_E2E_OIDC_CLEARTEXT_SESSION_SECRET=a-session-signing-secret-for-the-cleartext-provider
ONE_E2E_OIDC_ENCRYPTED_CLIENT_SECRET=a-client-secret-for-the-encrypted-provider
ONE_E2E_OIDC_ENCRYPTED_SESSION_SECRET=a-session-signing-secret-for-the-encrypted-provider
# The two policies gating /unavailable are each missing exactly one of the pair
# below, so the variable the other one needs has to be present for the refusal
# to be attributable to the absent one alone
ONE_E2E_OIDC_BROKEN_CLIENT_SECRET=a-client-secret-whose-policy-lacks-a-session-secret
ONE_E2E_OIDC_BROKEN_SESSION_SECRET=a-session-signing-secret-whose-policy-lacks-a-client-secret
# Shorter than a session secret is allowed to be, so the policy naming it can
# neither mint nor verify a cookie
ONE_E2E_OIDC_SHORT_SESSION_SECRET=too-short
# Two secrets, newest first, so a cookie signed under either is admitted while
# a fresh one is signed under the first alone
ONE_E2E_OIDC_ROTATED_SESSION_SECRET=the-session-secret-signing-from-now-on
ONE_E2E_OIDC_ROTATED_SESSION_SECRET_OLD=the-session-secret-it-replaced-but-honours
